apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "find-my-relative.fullname" . }}-test-connection"
  labels:
{{ include "find-my-relative.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "find-my-relative.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
