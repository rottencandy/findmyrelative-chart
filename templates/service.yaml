apiVersion: v1
kind: Service
metadata:
  name: {{ include "find-my-relative.fullname" . }}
  labels:
    {{- include "find-my-relative.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "find-my-relative.selectorLabels" . | nindent 4 }}
